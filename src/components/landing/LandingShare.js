import React from 'react'
import styled from 'styled-components'

const LandingShare = () => {
  return <Wrapper>LandingShare</Wrapper>
}

const Wrapper = styled.div`
  height: calc(100vh - 54px);
`
export default LandingShare
